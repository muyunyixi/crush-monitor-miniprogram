<view class="page">
  <view class="headline">微信多条消息粘贴验证</view>
  <view class="hint">在微信聊天中多选至少 3 条不同的文字消息并复制。切回本小程序，长按下方原生输入框，选择“粘贴”。</view>

  <view class="card">
    <view class="label">原生 textarea（不监听或改写粘贴事件）</view>
    <textarea class="editor" value="{{draft}}" bindinput="onInput" placeholder="长按这里，粘贴微信里多选复制的聊天消息" maxlength="-1" auto-height="false" cursor-spacing="24" show-confirm-bar="false" />
  </view>

  <view class="card stats">
    <view>字符数：<text class="number">{{length}}</text></view>
    <view>非空行：<text class="number">{{lines}}</text></view>
    <view class="small">记录会自动存到本机；退出小程序后再次打开检查是否保留。</view>
  </view>

  <view class="card">
    <view class="label">实际收到的完整内容</view>
    <view class="preview" selectable="true">{{preview}}</view>
  </view>

  <button class="clear" bindtap="clearDraft">清空记录</button>
  <view class="foot">v0.1.0 · 仅测试粘贴和本机保存，不发送聊天内容、不调用后端或 OCR。</view>
</view>
